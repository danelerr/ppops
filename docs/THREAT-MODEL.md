# PPOps threat model

The canonical repository-grounded report is
[ppops-threat-model.md](./ppops-threat-model.md). This compatibility path exists
because the v0.1 specification requires `docs/THREAT-MODEL.md` while the threat
model workflow names reports after the modeled repository.
